export interface ShoppingBasketItemResponse {
  productVariantId: number;
  productName: string;
  size: string;
  price: number;
  imageUrl: string;
  stock: number;
  color: string;
  quantity: number;
}
